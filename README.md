# dtrpg-sdk.swift

An SDK library for DriveThru RPG API in Swift

## OpenAPI source

The package uses the `dtrpg-api` repository as an `API` submodule.
`Sources/SDK/openapi.yaml` points at `API/openapi.yaml` for Swift OpenAPI code generation.
